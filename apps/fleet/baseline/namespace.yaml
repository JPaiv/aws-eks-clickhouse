# The fleet baseline — deployed to every fleet=spoke cluster by the
# spoke-baseline ApplicationSet: the namespace, the usable StorageClass and
# the Keeper quorum. ClickHouse itself arrives next.
apiVersion: v1
kind: Namespace
metadata:
  name: clickhouse
  labels:
    app.kubernetes.io/managed-by: argocd
